.container
  .jumbotron.jumbo_portfolio
    %h1 Contact
    .row
      - data.contacts.each do |contact|
        .col-sm-4
          %a{href: "#{contact.url}"}
            - display_social_icon(contact)
